<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dst.goodsmanager.mapper.AccountMapper">
  <select id="queryAccountList" resultType="Account">
    select * from account
  </select>
  <select id="test1" parameterType="String" resultType="String">
    select name from account where name=#{name}
  </select>
</mapper>